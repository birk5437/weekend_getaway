<%- model_class = GetawaySearch -%>
<div class="page-header">
  <h1><%=t '.title', :default => [:'helpers.titles.new', 'New %{model}'], :model => GetawaySearch %></h1>
</div>
<%= render :partial => 'form' %>

<h2>Recent Searches</h2>
<%= render partial: "list", locals: { getaway_searches: @getaway_searches } %>